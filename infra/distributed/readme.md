# Distributing

Whatever you choose to rep it in the sea, sql or even in amazonas as a pascal daatabase if that exists,

this folder is the convertion one.


registry is just data connected, it is however the base format* so instead of using sql etc, we create for now a automated migration

where:

both entityframework migrations work, then also core migration in the file, etc <->
